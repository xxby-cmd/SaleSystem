# Decisions

## 2026-09-06

- `approved_by: user`：下一学习单元应进入新知识，不把商品目录持久化补完冒充新单元。
- `approved_by: user`：后续可以由 AI 协助执行测试；代码存在和 AI 测试通过仍不自动等于用户独立掌握。
- `approved_by: user`：当前批量保存采用“错误前成功、遇到第一条数据库错误停止、后续不保存”的语义，逐行容错以后再扩展。
- `approved_by: user`：对象允许先创建后补充普通属性；产品型号允许纠错修改，但目标型号若已存在则拒绝。
- `approved_by: user`：内部价格可以保留多位小数，最终展示时再按规则保留两位。

